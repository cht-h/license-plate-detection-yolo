[project]
name = "ml2"
version = "0.1.0"
description = ""
authors = [
    {name = "karma",email = "chernikoaan@yandex.com"}
]
readme = "README.md"
requires-python = ">=3.14,<3.15"
dependencies = [
    "torch (>=2.10.0,<3.0.0)",
    "torchvision (>=0.25.0,<0.26.0)",
    "torchaudio (>=2.10.0,<3.0.0)",
    "ultralytics (>=8.4.24,<9.0.0)",
    "opencv-python (>=4.13.0.92,<5.0.0.0)",
    "numpy (>=2.4.3,<3.0.0)",
    "wandb (>=0.25.1,<0.26.0)",
    "pyyaml (>=6.0.3,<7.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"


[tool.poetry.scripts]
license-plate-detect = "main:main"
